export const AppConfig = {
  // The path to the scratch directory for temporary files
  SCRATCH_DIRECTORY: "/tmp",

  // string value indicating the type of storage to use for document templates
  STORAGE_TYPES: {
    AWS_S3: "aws.s3",
  },
};
